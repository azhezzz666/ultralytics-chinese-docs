| 参数                                                                                                   | 类型    | 默认值                  | 支持的任务                                     | 范围          | 描述                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------ | ------- | ----------------------- | ---------------------------------------------- | ------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`hsv_h`](../guides/yolo-data-augmentation.md/#hue-adjustment-hsv_h)                                   | `float` | `{{ hsv_h }}`           | `detect`, `segment`, `pose`, `obb`, `classify` | `0.0 - 1.0`   | 按色轮的一定比例调整图像的色调，引入颜色变化。帮助模型在不同光照条件下泛化。                                                                                    |
| [`hsv_s`](../guides/yolo-data-augmentation.md/#saturation-adjustment-hsv_s)                            | `float` | `{{ hsv_s }}`           | `detect`, `segment`, `pose`, `obb`, `classify` | `0.0 - 1.0`   | 按一定比例改变图像的饱和度，影响颜色的强度。用于模拟不同的环境条件。                                                                                            |
| [`hsv_v`](../guides/yolo-data-augmentation.md/#brightness-adjustment-hsv_v)                            | `float` | `{{ hsv_v }}`           | `detect`, `segment`, `pose`, `obb`, `classify` | `0.0 - 1.0`   | 按一定比例修改图像的明度（亮度），帮助模型在各种光照条件下表现良好。                                                                                            |
| [`degrees`](../guides/yolo-data-augmentation.md/#rotation-degrees)                                     | `float` | `{{ degrees }}`         | `detect`, `segment`, `pose`, `obb`             | `0.0 - 180`   | 在指定的角度范围内随机旋转图像，提高模型识别各种方向物体的能力。                                                                                                |
| [`translate`](../guides/yolo-data-augmentation.md/#translation-translate)                              | `float` | `{{ translate }}`       | `detect`, `segment`, `pose`, `obb`             | `0.0 - 1.0`   | 按图像尺寸的一定比例水平和垂直平移图像，有助于学习检测部分可见的物体。                                                                                          |
| [`scale`](../guides/yolo-data-augmentation.md/#scale-scale)                                            | `float` | `{{ scale }}`           | `detect`, `segment`, `pose`, `obb`, `classify` | `0 - 1`       | 按增益因子缩放图像，模拟距离相机不同距离的物体。                                                                                                                |
| [`shear`](../guides/yolo-data-augmentation.md/#shear-shear)                                            | `float` | `{{ shear }}`           | `detect`, `segment`, `pose`, `obb`             | `-180 - +180` | 按指定角度剪切图像，模拟从不同角度观看物体的效果。                                                                                                              |
| [`perspective`](../guides/yolo-data-augmentation.md/#perspective-perspective)                          | `float` | `{{ perspective }}`     | `detect`, `segment`, `pose`, `obb`             | `0.0 - 0.001` | 对图像应用随机透视变换，增强模型理解 3D 空间中物体的能力。                                                                                                      |
| [`flipud`](../guides/yolo-data-augmentation.md/#flip-up-down-flipud)                                   | `float` | `{{ flipud }}`          | `detect`, `segment`, `pose`, `obb`, `classify` | `0.0 - 1.0`   | 以指定概率上下翻转图像，在不影响物体特征的情况下增加数据多样性。                                                                                                |
| [`fliplr`](../guides/yolo-data-augmentation.md/#flip-left-right-fliplr)                                | `float` | `{{ fliplr }}`          | `detect`, `segment`, `pose`, `obb`, `classify` | `0.0 - 1.0`   | 以指定概率左右翻转图像，用于学习对称物体和增加数据集多样性。                                                                                                    |
| [`bgr`](../guides/yolo-data-augmentation.md/#bgr-channel-swap-bgr)                                     | `float` | `{{ bgr }}`             | `detect`, `segment`, `pose`, `obb`             | `0.0 - 1.0`   | 以指定概率将图像通道从 RGB 翻转为 BGR，用于增加对错误通道顺序的鲁棒性。                                                                                         |
| [`mosaic`](../guides/yolo-data-augmentation.md/#mosaic-mosaic)                                         | `float` | `{{ mosaic }}`          | `detect`, `segment`, `pose`, `obb`             | `0.0 - 1.0`   | 将四张训练图像合并为一张，模拟不同的场景组合和物体交互。对复杂场景理解非常有效。                                                                                |
| [`mixup`](../guides/yolo-data-augmentation.md/#mixup-mixup)                                            | `float` | `{{ mixup }}`           | `detect`, `segment`, `pose`, `obb`             | `0.0 - 1.0`   | 混合两张图像及其标签，创建合成图像。通过引入标签噪声和视觉变化来增强模型的泛化能力。                                                                            |
| [`cutmix`](../guides/yolo-data-augmentation.md/#cutmix-cutmix)                                         | `float` | `{{ cutmix }}`          | `detect`, `segment`, `pose`, `obb`             | `0.0 - 1.0`   | 组合两张图像的部分，创建部分混合同时保持不同区域。通过创建遮挡场景来增强模型鲁棒性。                                                                            |
| [`copy_paste`](../guides/yolo-data-augmentation.md/#copy-paste-copy_paste)                             | `float` | `{{ copy_paste }}`      | `segment`                                      | `0.0 - 1.0`   | 在图像之间复制和粘贴物体以增加物体实例。                                                                                                                        |
| [`copy_paste_mode`](../guides/yolo-data-augmentation.md/#copy-paste-mode-copy_paste_mode)              | `str`   | `{{ copy_paste_mode }}` | `segment`                                      | -             | 指定要使用的 `copy-paste` 策略。选项包括 `'flip'` 和 `'mixup'`。                                                                                                |
| [`auto_augment`](../guides/yolo-data-augmentation.md/#auto-augment-auto_augment)                       | `str`   | `{{ auto_augment }}`    | `classify`                                     | -             | 应用预定义的增强策略（`'randaugment'`、`'autoaugment'` 或 `'augmix'`）通过视觉多样性来增强模型性能。                                                            |
| [`erasing`](../guides/yolo-data-augmentation.md/#random-erasing-erasing)                               | `float` | `{{ erasing }}`         | `classify`                                     | `0.0 - 0.9`   | 在训练期间随机擦除图像区域，鼓励模型关注不太明显的特征。                                                                                                        |
| [`augmentations`](../guides/yolo-data-augmentation.md/#custom-albumentations-transforms-augmentations) | `list`  | `{{ augmentations }}`   | `detect`, `segment`, `pose`, `obb`             | -             | 用于高级数据增强的自定义 Albumentations 变换（仅限 Python API）。接受变换对象列表以满足专门的增强需求。                                                         |
